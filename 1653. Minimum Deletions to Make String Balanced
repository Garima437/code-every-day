class Solution {
    public int minimumDeletions(String s) {
        int dp=0;
        int cnt=0;
        for(char ch:s.toCharArray()){
            if(ch=='b') cnt++;
            else dp=Math.min(dp+1,cnt);
        }
        return dp;
    }
}
