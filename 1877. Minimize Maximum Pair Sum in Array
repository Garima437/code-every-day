class Solution {
    public int minPairSum(int[] nums) {
        int n=nums.length;
        int maxsum=0;
        Arrays.sort(nums);
        for(int i=0;i<n/2;i++){
            int pairSum=nums[i]+nums[n-1-i];
            maxsum=Math.max(maxsum,pairSum);
        }
        return maxsum;
    }
}
